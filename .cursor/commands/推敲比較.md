# 推敲比較（キャリブレーション）

台本 Markdown を `@ファイル` で添えるか、リポジトリ内のパスを1つ示す。

## 実行内容

1. `agents/calibrator.md` の手順に従う（これが検索・読込の起点）。
2. `config/quality.md` の「推敲比較（キャリブレーション）時の絶対ルール」を守る（文脈不足時は推測せず必ず質問）。
3. `config/calibration.md` の分割マーカーで A（マーカーより上＝AI原稿）と B（より下＝確定稿）とする。B が実質空なら、貼り付けを促して比較を止める。
4. 出力はチャットが既定。改善提案は `config/voice.md` に限定せず、差分の本質に応じて `config/brand.md`・`config/audience.md`・`templates/structure-*.md`・関連する `agents/*.md` など、どの SSOT をどう変えると再現性が上がるかを指名する。リポジトリファイルの自動マージや無承認での書き換えはしない。
5. `outputs/.../06-calibration.md` への保存は、利用者が明示したときのみ行う。

## 参照（必要に応じて開く）

- `agents/calibrator.md`
- `config/calibration.md`
- `config/quality.md`
